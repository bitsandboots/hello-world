# Hello

	mkdir <projectfolder>
	cd <projectfolder>
	git init
	git config --global user.name 'bitsandboots'
	git config --global user.email 'matthew.bray@gmail.com'
	git config --global color.ui 'auto'

Create one or more files, unless connecting to an existing directory
